import { QuestionTracker, generateSessionId, StudyQuestion, ScoreResult } from './app';

// You can now use these classes/types in this file to build the rest of your app logic in TypeScript.
// For example, you can implement the UI logic, API calls, and feedback rendering here.

// Example usage:
const tracker = QuestionTracker.getInstance();
const sessionId = generateSessionId();
console.log('Session ID:', sessionId);
